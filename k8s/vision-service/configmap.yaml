apiVersion: v1
kind: ConfigMap
metadata:
  name: vision-service-config
  namespace: receipt-understanding
data:
  # Nothing needed yet — Settings() defaults cover local/demo use.
  # Future: MODEL_S3_BUCKET / MODEL_S3_PREFIX for an init-container download step.
  PLACEHOLDER: "true"
