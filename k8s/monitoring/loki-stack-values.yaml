loki:
  persistence:
    enabled: false
  resources:
    requests:
      cpu: 100m
      memory: 256Mi

promtail:
  enabled: true

grafana:
  enabled: false
  sidecar:
    datasources:
      enabled: false   # stop loki-stack from provisioning its own competing datasource ConfigMap